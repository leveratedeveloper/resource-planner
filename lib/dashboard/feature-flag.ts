/** Single source of truth for whether the Insights dashboard is live.
 *  The dashboard is an unfinished feature: the home-screen entry button and the
 *  `/dashboard` route both read this flag. Flip to `true` to revive it. */
export const DASHBOARD_FEATURE_ENABLED = false;
